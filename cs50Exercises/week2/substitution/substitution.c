#include <cs50.h>#include <stdio.h>#include <string.h>#include <ctype.h>int charFoundInStringCase(char needle, string hayStack){    for (int i = 0; i < strlen(hayStack); i++)    {        if (hayStack[i] == needle)        {            return 1;        }    }    return 0;}int main(int argc, string argv[]){    if (argc != 2)    {        printf("Usage: ./substitution key\n");        return 1;    }    int argLength = strlen(argv[1]);    if (argLength != 26)    {        printf("Key must contain 26 characters.\n");        return 1;    }    for (int i = 0; i < argLength; i++)    {        if (!isalpha(argv[1][i]))        {            printf("Key must only contain alphabetic characters\n");            return 1;        }    }    for (int i = 0; i < 26; i++)    {        char UpperCaseASCII = i + 65;        char lowerCaseASCII = i + 97;        int lowerCaseLetterFound = charFoundInStringCase(lowerCaseASCII, argv[1]);        int upperCaseLetterFound = charFoundInStringCase(UpperCaseASCII, argv[1]);        if (lowerCaseLetterFound == 0 && upperCaseLetterFound == 0)        {            printf("Each letter of the alphabet must be present in the key\n");            return 1;        }    }    // get inputs    string plainText = get_string("plaintext: ");    printf("ciphertext: ");    for (int i = 0; i < strlen(plainText); i++)    {        char character = plainText[i];        if ((character >= 65 && character <= 90) || (character >= 97 && character <= 122))        {            // encrypt and print upper case character            if (character >= 65 && character <= 90)            {                char encryptedChar;                int characterPositionInKey = (int)character - 65;                encryptedChar = argv[1][characterPositionInKey];                // if it's lowercase, make it uppercase                if (encryptedChar >= 97)                    encryptedChar -= 32;                printf("%c", encryptedChar);            }            // encrypt and print lower case character            if (character >= 97 && character <= 122)            {                char encryptedChar;                int characterPositionInKey = (int)character - 97;                // TODO check case                encryptedChar = argv[1][characterPositionInKey];                // if it's uppercase, make it lowercase                if (encryptedChar <= 90)                    encryptedChar += 32;                // TODO                printf("%c", encryptedChar);            }        }        else        {            // print character unaltered            printf("%c", character);        }    }    printf("\n");    return 0;}